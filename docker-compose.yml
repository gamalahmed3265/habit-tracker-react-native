version: "3.8"
services:
  expo:
    build: .
    ports:
      - "8081:8081"
    volumes:
      - .:/app
    environment:
      - EXPO_DEVTOOLS_LISTEN_ADDRESS=0.0.0.0
      - NODE_ENV=development
    command: npx expo start --tunnel --clear
